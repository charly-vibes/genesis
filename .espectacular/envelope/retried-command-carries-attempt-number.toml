id = "retried-command-carries-attempt-number"
description = "enriched envelope round-trips through parse_envelope"
archetype = "CE"
status = "active"
superseded_by = ""
authored_with = "0.3.0"

[tests]
cargo = [
    { flags = "test_with_receipt_builder_round_trips_through_parse_envelope", timeout_seconds = 60 },
]
