id = "degradation-is-deterministic"
description = "bounded generation is deterministic"
archetype = "PF"
status = "active"
superseded_by = ""
authored_with = "0.3.0"

[tests]
cargo = [
    { flags = "degradation_is_deterministic", timeout_seconds = 60 },
]
