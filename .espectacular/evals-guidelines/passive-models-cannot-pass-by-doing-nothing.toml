id = "passive-models-cannot-pass-by-doing-nothing"
description = "a passive trajectory fails the empty-trajectory guard"
archetype = "NR"
status = "active"
superseded_by = ""
authored_with = "0.3.0"

[tests]
cargo = [
    { flags = "check_discipline_normative_content_is_published", timeout_seconds = 60 }
]
