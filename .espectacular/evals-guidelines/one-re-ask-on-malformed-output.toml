id = "one-re-ask-on-malformed-output"
description = "malformed output triggers exactly one re-ask with the format error"
archetype = "CE"
status = "active"
superseded_by = ""
authored_with = "0.3.0"

[tests]
cargo = [
    { flags = "live_action_protocol_normative_content_is_published", timeout_seconds = 60 }
]
