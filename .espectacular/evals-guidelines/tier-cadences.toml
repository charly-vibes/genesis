id = "tier-cadences"
description = "tier 0 every push, tier 1 nightly, tier 2 scheduled rotation with a cycle no longer than 14 days"
archetype = "CE"
status = "active"
superseded_by = ""
authored_with = "0.3.0"

[tests]
cargo = [
    { flags = "tier_ladder_normative_content_is_published", timeout_seconds = 60 }
]
