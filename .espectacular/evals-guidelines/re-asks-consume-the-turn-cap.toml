id = "re-asks-consume-the-turn-cap"
description = "the re-ask counts as one turn toward the cap"
archetype = "CE"
status = "active"
superseded_by = ""
authored_with = "0.3.0"

[tests]
cargo = [
    { flags = "live_action_protocol_normative_content_is_published", timeout_seconds = 60 }
]
