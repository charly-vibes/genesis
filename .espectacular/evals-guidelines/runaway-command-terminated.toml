id = "runaway-command-terminated"
description = "per-command timeout terminates runaway processes and records the timeout"
archetype = "SA"
status = "active"
superseded_by = ""
authored_with = "0.3.0"

[tests]
cargo = [
    { flags = "provisioning_and_sandbox_normative_content_is_published", timeout_seconds = 60 }
]
