id = "suite-wide-aggregation"
description = "rows under one report_version aggregate without per-tool mapping"
archetype = "CE"
status = "active"
superseded_by = ""
authored_with = "0.3.0"

[tests]
cargo = [
    { flags = "report_contract_normative_content_is_published", timeout_seconds = 60 },
    { flags = "report_fixtures_are_loadable_and_versioned", timeout_seconds = 60 }
]
