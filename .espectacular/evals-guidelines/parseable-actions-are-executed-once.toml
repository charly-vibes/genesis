id = "parseable-actions-are-executed-once"
description = "a parseable action is executed exactly once and recorded executed: true"
archetype = "SA"
status = "active"
superseded_by = ""
authored_with = "0.3.0"

[tests]
cargo = [
    { flags = "live_action_protocol_normative_content_is_published", timeout_seconds = 60 }
]
