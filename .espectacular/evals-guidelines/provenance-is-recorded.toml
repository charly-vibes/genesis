id = "provenance-is-recorded"
description = "every scenario names its observed failure source or declares itself a channel stress test"
archetype = "CE"
status = "active"
superseded_by = ""
authored_with = "0.3.0"

[tests]
cargo = [
    { flags = "check_discipline_normative_content_is_published", timeout_seconds = 60 }
]
