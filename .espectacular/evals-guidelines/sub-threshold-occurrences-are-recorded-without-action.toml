id = "sub-threshold-occurrences-are-recorded-without-action"
description = "below-threshold agent faults are recorded without routing action"
archetype = "CE"
status = "active"
superseded_by = ""
authored_with = "0.3.0"

[tests]
cargo = [
    { flags = "fault_routing_normative_content_is_published", timeout_seconds = 60 }
]
