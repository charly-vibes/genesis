id = "tool-fault-becomes-a-ticket"
description = "tool faults are filed as tickets in the tool's tracker"
archetype = "CE"
status = "active"
superseded_by = ""
authored_with = "0.3.0"

[tests]
cargo = [
    { flags = "fault_routing_normative_content_is_published", timeout_seconds = 60 }
]
