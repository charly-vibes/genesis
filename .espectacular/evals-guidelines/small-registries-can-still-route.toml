id = "small-registries-can-still-route"
description = "fewer than three ids can still route when the code appears across all of them"
archetype = "CE"
status = "active"
superseded_by = ""
authored_with = "0.3.0"

[tests]
cargo = [
    { flags = "fault_routing_normative_content_is_published", timeout_seconds = 60 }
]
