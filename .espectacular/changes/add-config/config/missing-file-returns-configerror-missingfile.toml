id = "missing-file-returns-configerror-missingfile"
description = "missing file returns configerror missingfile"
archetype = "SA"
status = "active"
superseded_by = ""
authored_with = "0.3.0"

[tests]
cargo = [
    { flags = "test_config_file_read_missing", timeout_seconds = 60 },
]
