id = "managed-block-markers-follow-the-config-start-end-convention"
description = "managed block markers follow the config start end convention"
archetype = "SA"
status = "active"
superseded_by = ""
authored_with = "0.3.0"

[tests]
cargo = [
    { flags = "test_registry_marker", timeout_seconds = 60 },
]
