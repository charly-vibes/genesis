id = "managed-block-generates-a-table-of-found-and-missing-configs"
description = "managed block generates a table of found and missing configs"
archetype = "SA"
status = "active"
superseded_by = ""
authored_with = "0.3.0"

[tests]
cargo = [
    { flags = "test_config_store_managed_block", timeout_seconds = 60 },
]
