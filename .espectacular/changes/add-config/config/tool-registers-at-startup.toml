id = "tool-registers-at-startup"
description = "tool registers at startup"
archetype = "SA"
status = "active"
superseded_by = ""
authored_with = "0.3.0"

[tests]
cargo = [
    { flags = "test_registry_register_and_check", timeout_seconds = 60 },
]
