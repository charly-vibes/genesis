id = "discover-finds-all-registered-configs-in-a-repo"
description = "discover finds all registered configs in a repo"
archetype = "SA"
status = "active"
superseded_by = ""
authored_with = "0.3.0"

[tests]
cargo = [
    { flags = "test_config_store_discover_all_found", timeout_seconds = 60 },
]
