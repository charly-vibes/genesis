id = "output-with-json-flag-produces-envelope"
description = "output with json flag produces envelope"
archetype = "SA"
status = "active"
superseded_by = ""
authored_with = "0.3.0"

[tests]
cargo = [
    { flags = "test_to_envelope_serializes_to_json", timeout_seconds = 60 },
]
