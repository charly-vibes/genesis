id = "run-returns-exit-code"
description = "run returns exit code"
archetype = "SA"
status = "active"
superseded_by = ""
authored_with = "0.3.0"

[tests]
cargo = [
    { flags = "test_run_failure_exit_code", timeout_seconds = 60 },
]
