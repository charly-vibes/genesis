id = "with-toml-writes-a-valid-toml-file"
description = "with toml writes a valid toml file"
archetype = "SA"
status = "active"
superseded_by = ""
authored_with = "0.3.0"

[tests]
cargo = [
    { flags = "test_with_toml_creates_valid_toml", timeout_seconds = 60 },
]
