id = "assert-file-contains-passes-for-matching-content"
description = "assert file contains passes for matching content"
archetype = "SA"
status = "active"
superseded_by = ""
authored_with = "0.3.0"

[tests]
cargo = [
    { flags = "test_with_file_creates_file", timeout_seconds = 60 },
]
