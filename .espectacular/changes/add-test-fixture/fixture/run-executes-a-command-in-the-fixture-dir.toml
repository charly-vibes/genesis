id = "run-executes-a-command-in-the-fixture-dir"
description = "run executes a command in the fixture dir"
archetype = "SA"
status = "active"
superseded_by = ""
authored_with = "0.3.0"

[tests]
cargo = [
    { flags = "test_run_echo_command", timeout_seconds = 60 },
]
