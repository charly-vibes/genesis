id = "with-file-creates-a-file-with-content"
description = "with file creates a file with content"
archetype = "SA"
status = "active"
superseded_by = ""
authored_with = "0.3.0"

[tests]
cargo = [
    { flags = "test_with_file_creates_file", timeout_seconds = 60 },
]
