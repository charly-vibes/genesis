id = "with-git-init-creates-a-git-repo"
description = "with git init creates a git repo"
archetype = "SA"
status = "active"
superseded_by = ""
authored_with = "0.3.0"

[tests]
cargo = [
    { flags = "test_with_git_init_creates_repo", timeout_seconds = 60 },
]
