id = "fixture-path-resolves-relative-paths"
description = "fixture path resolves relative paths"
archetype = "SA"
status = "active"
superseded_by = ""
authored_with = "0.3.0"

[tests]
cargo = [
    { flags = "test_path_resolution", timeout_seconds = 60 },
]
