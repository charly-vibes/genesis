## 1. Crate skeleton
- [ ] 1.1 Populate the existing `genesis` repo (remote `git@cv:charly-vibes/genesis.git`) with `Cargo.toml`, `src/lib.rs`, `justfile`, `lefthook.yml`, `_typos.toml`, `book.toml`, `llms.txt`/`llm.txt`, `AGENTS.md` (following the tool-craft §8 template). The repo and `openspec/` already exist; do not recreate or clobber them.
- [ ] 1.2 Define `src/lib.rs` re-exporting the six modules (initially empty stubs).
- [ ] 1.3 Add CI (fmt-check + clippy + test + build-release) mirroring the suite `just ci`.
- [ ] 1.4 Tag `v0.1.0` once stubs compile.

## 2. Extract `envelope` (supersedes dont-2j6o)
- [ ] 2.1 Port `dont/src/envelope.rs` (`Envelope`, `EnvelopeKind`, `ErrorResult`) into `genesis/src/envelope.rs` carrying dont-2j6o's design notes verbatim.
- [ ] 2.2 Make the `data` field generic over `T: Serialize`.
- [ ] 2.3 Add conformance tests for the envelope shape (ok, envelope_version, cli_version, envelope_kind, data, warnings, hints, meta).
- [ ] 2.4 Close `dont-2j6o` as superseded **after** its child adoption issues (wai/pretender/espectacular/testaruda envelope adoption) are re-pointed at the per-repo `adopt-genesis` proposals, so none are orphaned.

## 3. Extract `suggestions` (donor: wai)
- [ ] 3.1 Port `wai/src/suggestions.rs` (`Suggestion` enum, `SuggestionEngine`, `suggest_typo`/`suggest_order`/`suggest_context`) into `genesis/src/suggestions.rs`.
- [ ] 3.2 Add a `footer()` helper that formats a `Suggestion` as the "→ Run: …" footer string.
- [ ] 3.3 Add a hook point so a tool can register its valid-command list and get typo detection for free.

## 4. Extract `managed_block` (donors: wai/dont/espectacular)
- [ ] 4.1 Port the `<!-- …:START -->`/`<!-- …:END -->` injector from `wai/src/managed_block.rs` into `genesis/src/managed_block.rs`.
- [ ] 4.2 Generalize the block registry so any tool can register a named block (WAI, OPENSPEC, DONT, ah:managed, …).
- [ ] 4.3 Preserve the slim-block Layer-1 progressive-disclosure behavior.

## 5. Build `feedback` (new)
- [ ] 5.1 Implement the redactor (whole-body, value-not-key-substring matching, `git_remote` host/path reduction) per `agent-issue-reporting.md` §5.
- [ ] 5.2 Implement the context-bundle serializer per §3.
- [ ] 5.3 Implement the error-scratch JSONL writer (best-effort, read-only/temp fallback, never shadows the real error) per §4.
- [ ] 5.4 Implement the `gh` invocation + fallback ladder (missing/unauthed/labels/no-network/permission) per §7.

## 6. Build `suite_linter` (wai-bdqw.8)

The suite_linter is an **orchestrator, not a monolith**. Each tool defines
its own checks via the `LintCheck` trait; genesis just runs them.

- [ ] 6.1 Define the `LintCheck` trait (fn signature, result type, fix fn).
- [ ] 6.2 Define a `LinterRegistry` where tools register their checks at startup.
- [ ] 6.3 Implement `LinterRegistry::run_all()` — iterates, runs, collects results.
- [ ] 6.4 Implement `LinterRegistry::run_named(name)` — run a single check by name.
- [ ] 6.5 Add `LintResult` type with severity (advisory/warning/error) and optional fix command.

Tool-specific checks (testaruda.toml schema, pretender.toml presence, ah/dont
wiring, badge↔block match) belong in each tool's own `doctor` command, not
in genesis. Each tool registers its checks with the `LinterRegistry`.

## 7. AIX generation (`aix` module)
- [ ] 7.1 Extract `llms.txt`/`llm.txt` generation from wai into genesis.
- [ ] 7.1b Genesis bootstrap: genesis's own AIX artifacts are generated by genesis once `aix` is stable; until then, hand-write a minimal `AGENTS.md` (this foundation change suffices) rather than blocking the build on self-generation.
- [ ] 7.2 Provide an `agents_block(name, body)` helper for managed-block injection.

## 8. Stabilize and pin
- [ ] 8.1 Tag `v0.1.0` with the four extracted modules (`envelope`, `suggestions`, `managed_block`, `aix`) stable.
- [ ] 8.2 Verify `.wai/projects/genesis-foundation/research/tool-craft.md` Appendix A is accurate once dependents adopt; file a charly-monorepo ticket if not (do not edit the charly repo from this change).
- [ ] 8.3 Open the per-repo adoption proposals (one per tool repo) — they are blocked until this tag.
